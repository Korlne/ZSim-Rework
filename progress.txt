## Codebase Patterns
- Tauri v2 on Windows: `icons/icon.ico` is required by `tauri-build` even for `cargo check` — a minimal valid ICO can be generated from raw BGRA pixel bytes via Python `struct.pack`
- Tauri v2 uses `tauri-plugin-shell` for sidecar (not the v1 `tauri::api::process::Command`)
- Tauri v2 commands: define `#[tauri::command] fn` in `lib.rs`, register via `generate_handler![]`, manage state with `app.manage()` + `app.state::<T>()`
- Vite + Vanilla JS is the minimal frontend setup: `vite.config.js`, `index.html` at root, JS in `src/`
- `src-tauri/` can be a Rust workspace member without conflicts on Windows
- Sidecar processes: manage via `tauri::State` with `Mutex<Option<Child>>` + `Mutex<Option<ChildStdin>>`
- **Import module pattern**: Each data type has its own `import_<type>()` function in `import.rs`. They take `&Connection` and `data_dir: &Path`, return `ImportResult { success, errors }`. Use zsim-core's existing entity structs for JSON deserialization (they already have correct serde annotations for the data files). Convert enums to strings via `serde_json::to_value()` + `as_str()`. Store complex nested data (constellations, resistances, tracks) as JSON TEXT in SQLite. Use `INSERT OR REPLACE` for idempotent reimport.
- **Data entry Tauri commands**: DataDirState (managed via `.manage(DataDirState { data_dir })`) shares the data directory path across all data entry commands. Access via `State<'_, DataDirState>` parameter. Open a fresh `rusqlite::Connection` per command for infrequent ops (init, import). Always enable `PRAGMA foreign_keys = ON;` after opening each connection. Define commands directly in `lib.rs` and register in `generate_handler![]`.
- **Editor frontend structure**: The `src/editor/` module uses hash-based routing (`#/dashboard`, `#/characters`, etc.) with lazy-loaded page modules via dynamic `import()`. Each page exports `renderPage(route)` and is a separate Vite chunk. `utils/api.js` wraps all Tauri `invoke()` calls with JSON parsing. Page stubs use `editor.comingSoon` i18n key for placeholder content. i18n keys use `editor.nav.*` for navigation, `editor.dashboard.*` for dashboard UI.
- **View switching (simulation vs editor)**: The `index.html` has tab buttons that toggle `.hidden` class between `#simulation-view` and `#editor-view`. The editor is lazy-loaded on first tab click via dynamic `import("/src/editor/app.js")`. The `.view-panel` CSS class provides shared flex-column layout for both views.

---
## 2026-05-02 23:45 - US-019
- Implemented zsim-pyo3 crate with 4 Python-callable functions delegating to zsim-parquet aggregator
- aggregate(parquet_path, query_json): deserializes AggQuery from JSON, calls ParquetAggregator::aggregate, returns JSON string
- summary(parquet_path): convenience wrapper querying StatsSummary
- dps_curve(parquet_path, window_ticks): convenience wrapper querying DPS with sliding window
- damage_breakdown(parquet_path): convenience wrapper querying DamageBreakdown
- All functions return JSON strings (not Python objects) and use PyIOError for error reporting
- Workspace pyo3 0.24 with extension-module feature (.pyd/.so/.dylib output)
- Aggregation logic is fully tested by zsim-parquet's 18+ tests (pyo3 integration tests require `maturin develop`)
- Files changed: zsim-pyo3/Cargo.toml (+anyhow dep), zsim-pyo3/src/lib.rs (stub→real impl), scripts/ralph/prd.json (US-019 passes:true), progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: PyO3 `#[pyfunction]` annotations produce regular Rust functions that return `PyResult<T>` — the pyo3 layer is trivially thin (just wraps the aggregator + serialization).
  - Gotchas encountered: `cdylib` crate test binaries need Python DLL on Windows at runtime — cannot run Rust unit tests that call `#[pyfunction]` functions in a cdylib crate. Solution: keep pyo3 functions as thin wrappers and test the underlying logic in the dependency crate (zsim-parquet).
  - Useful context: Adding `anyhow` as a direct dependency of zsim-pyo3 is needed for the `to_py_err` conversion. zsim-parquet's aggregator provides all the aggregation logic — zsim-pyo3 is purely a bridging layer. Integration testing requires `maturin develop` to build the .pyd and test from Python.
---
## 2026-05-02 23:56 - US-021
- Implemented Tauri v2 project scaffolding with 3 IPC commands and vanilla JS/CSS frontend
- `src-tauri/Cargo.toml` with tauri 2.11 / tauri-build 2.6 / tauri-plugin-shell 2.3 deps
- `src-tauri/tauri.conf.json` configured for 1280×800 window, title "ZSim Analyzer", Vite dev server on :1420
- `src-tauri/src/lib.rs`: 3 `#[tauri::command]` functions (run_simulation stub, spawn_sidecar with Python child process, send_to_sidecar with stdin pipe)
- SidecarState managed via `tauri::State` with `Mutex<Option<Child>>` + `Mutex<Option<ChildStdin>>`
- `src-tauri/capabilities/default.json` with core:default + shell permissions
- Frontend: `package.json` (@tauri-apps/api ^2, @tauri-apps/plugin-shell ^2, vite ^6), `index.html`, `src/main.js`, `src/styles.css`
- Dark-themed UI with config panel (6 inputs), run/stop buttons, progress bar, sidecar controls, results panel
- Generated `icon.ico` for Windows tauri-build requirement (32×32 BGRA via Python)
- Added src-tauri to workspace members in root Cargo.toml
- Updated .gitignore for node_modules/ and dist/
- **Learnings for future iterations:**
  - Patterns discovered: Tauri v2 on Windows unconditionally requires `icons/icon.ico` for the build script — even `cargo check` fails without it. Generate with Python struct + raw BGRA bytes.
  - Gotchas encountered: The acceptance criterion references `tauri::api::process::Command::new_sidecar()` which is Tauri v1 API. Tauri v2 uses `tauri-plugin-shell` for sidecar management. Implemented with `std::process::Command` wrapped in `tauri::State` for the scaffold.
  - Useful context: `src-tauri/` works fine as a Rust workspace member. `beforeDevCommand` runs from the workspace root. The `dist/` directory must exist at project root for `cargo tauri build`. Tauri v2 capabilities/permissions are in `src-tauri/capabilities/default.json` using the standard permission identifiers.
  - Frontend context: Vite config needs `envPrefix: ["VITE_", "TAURI_"]` and `server.strictPort: true` for Tauri integration. The `@tauri-apps/api/core` `invoke()` function handles IPC to Rust commands. Tauri events use `listen()` from `@tauri-apps/api/event`.
---
## 2026-05-02 23:56 - US-002
- Created `src-tauri/src/data_entry/import.rs` with 5 import functions
- `import_characters`: reads `data/characters/*.json` via zsim-core `Character`, flattens `base_stats` into individual columns, stores constellations/action_dict as JSON TEXT
- `import_skills`: reads `data/skills/{char_id}.json` via zsim-core `SkillData`, inserts skills + `skill_multipliers` rows per skill with FK cascade
- `import_equipment`: reads `data/equipment/equipment.json` via zsim-core `EquipmentData`, splits across 3 tables (w_engines, drive_discs, disc_sets)
- `import_enemies`: reads `data/enemies/*.json` via zsim-core `EnemyState`, stores resistances/weaknesses/anomaly_buildup as JSON TEXT
- `import_apl`: reads `data/apl/*.json` via zsim-core `APLData`, stores tracks as JSON TEXT
- Added `zsim-core` as dependency of `src-tauri/Cargo.toml`
- Added `tempfile` dev-dependency for tests
- All import functions use `INSERT OR REPLACE` for idempotent reimport
- Error handling: per-file try/catch with descriptive error messages, continues on malformed files
- **Learnings for future iterations:**
  - Patterns discovered: zsim-core entity structs (Character, SkillData, EquipmentData, EnemyState, APLData) can be reused directly for JSON deserialization in the Tauri crate since they have correct serde annotations.
  - Gotchas encountered: `EnemyState` has no `level` field (not in JSON), so default 60 is used for the SQLite column. `BaseStats.dmg_bonus` maps to `energy_gen_rate` field in character JSON files via serde alias. zsim-core's `Character` has enum fields (`FactionTag`, `SpecialtyTag`, `ElementTag`) that serialize to their serde-rename strings — need `serde_json::to_value()` to extract the string value, not `format!("{:?}")`.
  - Useful context: The `ImportResult` struct with `success` + `errors` fields provides structured feedback suitable for the Tauri command response. All 11 tests use in-memory SQLite with `tempfile::TempDir` for isolated file I/O.
---
## 2026-05-02 23:58 - US-003
- Created 3 Tauri commands in `src-tauri/src/lib.rs`:
  - `init_database`: opens/creates `zsim.db` in data directory, calls `data_entry::db::init_db` to create all tables, returns table list as JSON
  - `import_from_json`: accepts `data_type` (characters/skills/equipment/enemies/apl + singular aliases) and optional `data_path`, delegates to import module functions, returns success count + errors
  - `reimport_all`: clears all tables in FK-safe order (child tables first), then calls `import_all` to re-import from data directory, returns per-type summary
- Added `DataDirState` (holds `data_dir: PathBuf`) managed as Tauri state
- Added `use std::path::PathBuf` and `use rusqlite::Connection` imports to lib.rs
- Commands open connection per-invocation (no persistent connection — these are infrequent operations)
- All commands use parameterized SQL (no string interpolation)
- All 17 existing unit tests pass
- **Learnings for future iterations:**
  - Patterns discovered: Tauri state for shared config (`DataDirState`) combined with per-command `Connection::open()` works well for infrequent DB ops. Register state via `.manage()` and access via `State<'_, T>` parameter in command functions.
  - Gotchas encountered: SQLite FK enforcement requires `PRAGMA foreign_keys = ON` per-connection (rusqlite doesn't enable by default). DELETE order matters: child tables (skill_multipliers) must be cleared before parent tables (characters) even with ON DELETE CASCADE, because FK constraints block parent deletion if children exist.
  - Useful context: The `data_dir` for dev is `CWD/data/`, computed at app startup and stored in `DataDirState`. The `import_from_json` command accepts both plural ("characters") and singular ("character") data_type values for flexibility.
---
## 2026-05-03 00:15 - US-005
- Implemented character CRUD Rust backend: `src-tauri/src/data_entry/characters.rs`
  - `get_characters`: returns all characters as JSON array, ordered by char_id
  - `get_character`: returns single character with associated skills as `{ character, skills }` JSON
  - `save_character`: INSERT OR REPLACE with ON CONFLICT DO UPDATE, accepts JSON matching CharacterRecord struct
  - `delete_character`: delete by char_id with ON DELETE CASCADE for skills and multipliers
  - Input validation: char_id format (lowercase+digits+underscores), level (1-60), ascension (0-6), crit fields (0.0-1.0), non-negative numeric fields
- Added 4 Tauri commands in `src-tauri/src/lib.rs` and registered in `generate_handler![]`
- Added `pub mod characters;` to `data_entry/mod.rs`
- Updated `scripts/ralph/prd.json` with complete data entry project PRD (US-001 through US-015), marked first 4 as passing
- Fixed pre-existing compilation errors in `zsim-core/src/data/loader.rs` (type mismatch in SQLite loading code)
- 16 unit tests for character CRUD (all pass), 448 tests pass across workspace
- **Learnings for future iterations:**
  - Patterns discovered: Flat CharacterRecord struct (matching DB columns directly) is simpler than zsim-core's nested Character. Tauri commands open per-invocation Connection — consistent with existing pattern from US-003. INSERT OR REPLACE with ON CONFLICT DO UPDATE works well for upsert and lets SQLite handle the UPDATE of updated_at.
  - Gotchas encountered: The `data_entry/mod.rs` already existed with `db` and `import` modules — was careful not to overwrite. The `CharacterWithSkills` struct needs `serde::Deserialize` for test deserialization (initially only had `Serialize`).
  - Useful context: Character validation rules are defined in the PRD for the data entry frontend. The existing `db.rs` schema had all the columns needed. The zsim-core loader already had `rusqlite` dependency (from prior work), so no Cargo.toml changes needed.
---
## 2026-05-03 00:25 - US-006
- Implemented skill CRUD Rust backend: `src-tauri/src/data_entry/skills.rs`
  - `get_skills`: returns all skills for a character with embedded multiplier segments, ordered by action_id
  - `save_skill`: accepts JSON with skill fields + multipliers array, uses transaction to atomically upsert skill and replace multipliers (delete-all + re-insert)
  - `delete_skill`: delete by skill id with ON DELETE CASCADE for multipliers
  - Validation: char_id, action_id, action_type required
- Added 3 Tauri commands in `src-tauri/src/lib.rs` and registered in `generate_handler![]`
- Added `pub mod skills;` to `data_entry/mod.rs`
- 9 unit tests for skill CRUD (all pass), full workspace 415+ tests pass
- **Learnings for future iterations:**
  - Patterns discovered: Skill CRUD follows the same pattern as character CRUD — flat structs matching DB columns, per-command Connection, transaction with BEGIN/COMMIT/ROLLBACK for atomic skill + multiplier writes. BOOL fields in SQLite stored as INTEGER, converted via `row.get::<_, i32>(n)? != 0`.
  - Gotchas encountered: `last_insert_rowid()` works correctly with `ON CONFLICT DO UPDATE` in SQLite 3.35+ — returns the rowid for both insert and update paths. Must use `ON CONFLICT(char_id, action_id)` (the UNIQUE constraint columns), not the PK id.
  - Useful context: Multiplier replacement strategy (DELETE all + re-insert) is simpler than diff-based updates and safe within a transaction. The `SkillWithMultipliers` struct is reused for both GET responses and SAVE input. Multiplier `decay_coeff` is Option<f64> to handle DB NULLs with serde default.
---
## 2026-05-03 00:35 - US-007
- Implemented equipment CRUD Rust backend: `src-tauri/src/data_entry/equipment.rs`
  - W-Engine CRUD: `get_w_engines`, `save_w_engine`, `delete_w_engine`
  - Drive Disc CRUD: `get_drive_discs`, `save_drive_disc`, `delete_drive_disc` (slot validation 1-6)
  - Disc Set CRUD: `get_disc_sets`, `save_disc_set`, `delete_disc_set`
  - `get_all_equipment`: combined query returning all three types as `{ w_engines, drive_discs, disc_sets }`
  - Validation: id/name/set_id required, slot 1-6 range check
- Added `pub mod equipment;` to `data_entry/mod.rs`
- Added 9 Tauri commands in `src-tauri/src/lib.rs` and registered in `generate_handler![]`
- 13 unit tests for equipment CRUD (all pass), full workspace 470+ tests pass
- **Learnings for future iterations:**
  - Patterns discovered: Equipment CRUD follows the same flat-struct + per-command Connection pattern. The `AllEquipment` wrapper struct provides a single endpoint for the frontend to load all equipment data at once.
  - Gotchas encountered: Drive disc has no FK constraint to disc_sets in the schema, so disc set lookup failures must be handled at the application layer. The sub_stats columns are nullable (4 pairs of name+value).
  - Useful context: Equipment data has 3 distinct types but only one file (equipment.json) — the CRUD reflects separating them into independent tables. `get_all_equipment` is convenient for dashboard loading but individual getters are more efficient for targeted updates.
---
## 2026-05-03 00:45 - US-008
- Implemented enemy CRUD Rust backend: `src-tauri/src/data_entry/enemies.rs`
  - `get_enemies`: returns all enemies as JSON array (summary fields: enemy_id, type, name, level, hp, def, base_res)
  - `get_enemy(enemy_id)`: returns full enemy record with resistances/weaknesses/anomaly_buildup as parsed JSON strings
  - `save_enemy`: INSERT OR REPLACE with ON CONFLICT DO UPDATE, validation (enemy_id format, level 1-60, base_res 0.0-1.0)
  - `delete_enemy`: delete by enemy_id
- Implemented data query module: `src-tauri/src/data_entry/queries.rs`
  - `get_data_summary`: COUNT queries across 6 tables (characters, skills, w_engines, drive_discs, disc_sets, enemies, apl) returning JSON object
  - `search_data(query, data_type)`: LIKE search across characters/skills/enemies/w_engines/disc_sets, returns flat `{ data_type, id, name }` results, supports "all" to search everything
- Added `pub mod enemies;` and `pub mod queries;` to `data_entry/mod.rs`
- Added 6 Tauri commands (get_enemies, get_enemy, save_enemy, delete_enemy, get_data_summary, search_data) to lib.rs and registered in generate_handler![]
- 13 unit tests (7 enemy + 6 queries) pass, full workspace 470+ tests pass
- **Learnings for future iterations:**
  - Patterns discovered: Enemies CRUD follows same pattern as characters/equipment. Data queries module is separate from entity CRUD because it spans multiple tables. search_data uses flat SearchResult struct with data_type discriminator for unified response.
  - Gotchas encountered: LIKE search needs `%` pattern wrapping. The `search_data` function needs to handle `data_type: "all"` by running queries against ALL tables and merging results. Different tables have different ID column names (char_id, enemy_id, id, set_id) — unified as `id` in SearchResult.
  - Useful context: The queries module is a standalone module that only depends on rusqlite and Connection — no entity structs needed. get_data_summary uses a closure factory pattern for concise COUNT queries.
---
## 2026-05-03 01:15 - US-009
- Created `src/editor/` with 7 modules + 6 page stubs:
  - `utils/api.js`: comprehensive API wrapper for all 20+ Tauri invoke calls (db, characters, skills, equipment, enemies, queries)
  - `app.js`: hash-based router with lazy-loaded page modules (dashboard, characters, skills, equipment, enemies, import)
  - `sidebar.js`: nav sidebar with Dashboard, Characters, Equipment (sub-items: W-Engines, Drive Discs, Disc Sets), Enemies, Import
  - `datatable.js`: reusable sortable data table component with column defs, format functions, empty state, edit/delete action column
  - `form.js`: reusable form builder with text/number/select/textarea/checkbox/tag-input field types, validation-ready value state
  - `confirm.js`: promise-based confirmation dialog with overlay, keyboard/click-to-dismiss
  - `styles.css`: editor-specific dark theme (sidebar, data-table, form, tags, tabs, dashboard cards, notifications, page states)
- Added `src/editor/pages/`:
  - `dashboard.js`: live data summary with record count cards, Init Database + Reimport All buttons
  - `characters.js`, `skills.js`, `equipment.js`, `enemies.js`, `import.js`: placeholder stubs with `Coming soon` text
- Modified `index.html`:
  - Added editor stylesheet link
  - Added view tab navigation (Simulation / Data Editor)
  - Renamed `#main` → `#simulation-view` with `.view-panel` class
  - Added `#editor-view` with `.editor-layout` + content containers
  - Added inline script for tab switching with lazy editor init (dynamic import of app.js)
- Extended all 3 locale files (en-US, zh-CN, ja-JP) with 30+ editor namespace keys
- **Learnings for future iterations:**
  - Patterns discovered: Hash-based routing with lazy-loaded page modules via `import()` keeps the editor shell lightweight -- each page is a separate Vite chunk loaded on demand. The app.js registers `hashchange` and `langchange` listeners once at init, page modules simply export a `renderPage(route)` function.
  - Gotchas encountered: When renaming `#main` to `#simulation-view`, existing CSS selector must be updated to `.view-panel` class selector. The inline tab-switching script in index.html uses dynamic `import()` for lazy editor loading -- must handle the case where the editor fails to load.
  - Useful context: Editor i18n keys use `editor.nav.*` prefix for nav labels, `editor.dashboard.*` for dashboard UI. CSS is scoped under `.editor-*` and `.data-table` classes to avoid conflicts with simulation styles. Tab switching toggles `.hidden` class between views.
---
## 2026-05-03 01:25 - US-009 (restructure)
- Moved component files to `src/editor/components/` subdirectory per PRD spec: sidebar.js → components/sidebar.js, datatable.js → components/datatable.js, form.js → components/form.js, confirm.js → components/confirm.js
- Created `src/editor/utils/validation.js`: reusable validation functions (charId, enemyId, level, ascension, critRate, nonNegative, required, slot, equipmentId, setId) + `validate()` and `validateForm()` combinators
- Created `src/editor/utils/format.js`: reusable formatters (formatNumber, formatPercent, formatFixed, formatStat, formatEnergy, truncate, formatList, formatJson)
- Enhanced components: datatable.js now supports onRowClick + custom emptyMessage; form.js supports custom save/cancel labels; confirm.js supports custom button labels + variant (danger/primary) + auto-focus
- Updated app.js import path: `./sidebar.js` → `./components/sidebar.js`
- All component imports updated from `../i18n.js` to `../../i18n.js` for the deeper component path
- cargo check passes, vite build succeeds (24 modules transformed, 0 errors)
- **Learnings for future iterations:**
  - Patterns discovered: Component files should live in `src/editor/components/` and pages in `src/editor/pages/` — app.js imports from `./components/` while page files import from `../../i18n.js` and `../utils/api.js`.
  - Gotchas encountered: When moving files to a deeper directory, import paths for `i18n.js` must be adjusted (one more `../` level). The vite build catches broken imports — always run `npx vite build` to verify after restructuring.
  - Useful context: Validation utilities use a simple `validate(value, validators)` combinator pattern and `validateForm(fieldMap)` for batch form validation. Format utilities are stateless pure functions suitable for use in datatable column `format` callbacks.
---
## 2026-05-03 01:45 - US-010
- Completed frontend character management page implementation
- Characters list table with faction/specialty/element filter dropdowns, sortable columns
- Inline edit panel with 2-column form: basic info (char_id, name, faction/specialty/element selects, level, ascension) + 12 base stats + 6 cinema toggle buttons + action list tag input
- Cinemas support both boolean and string array formats from DB, stringifies to `["cinema1","cinema2"]` format
- Validation on save: char_id format, level 1-60, ascension 0-6, crit rates/dmg 0-1, non-negative stats
- Delete with confirmation dialog, notifications for save/delete/error
- Created `src/editor/components/modal.js` modal dialog component (used by characters page)
- Updated datatable.js with i18n import for button labels (falls back to English hardcoded labels)
- Fixed locale files (en-US, zh-CN, ja-JP) with all flat-style i18n keys needed by character page
- All 3 locale files updated with consistent key naming pattern
- vite build passes (28 modules, 0 errors)
- Files changed: src/editor/pages/characters.js, src/editor/components/modal.js (new), src/editor/components/datatable.js, src/locales/*.json, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Patterns discovered: Inline edit panels (append below table, remove on save/cancel) are preferred over modals for complex forms with 20+ fields. Cinema data stored as string arrays `["cinema1","cinema2"]` — parseCinemas handles both boolean and string formats from DB.
  - Gotchas encountered: When locale files are restructured from dotted keys to flat keys, ALL usages must be updated in sync. The datatable hardcodes Edit/Del buttons — these should be i18n-ized for full localization. LF/CRLF warnings on Windows are cosmetic in git.
  - Useful context: The ralph/ submodule prd.json already had US-010 as passing — the scripts/ralph/prd.json copy needed syncing. Outer project and submodule have separate prd.json files that must both be updated.
---
## 2026-05-03 02:30 - US-011
- Marked US-011 (frontend skill editor page) as passing in PRD — the implementation was already completed in the same commit as US-010 (commit bce3a98) but the PRD was not updated
- skills.js has all 7 acceptance criteria: character dropdown with skill loading, skill table (action_id/type/energy/cooldown/actions), inline edit panel with all basic params, dynamic multiplier rows (add/delete segment/frame/multiplier/decay), effect_id free input, prerequisite action dropdown from existing skills, save/cancel/delete with confirmation
- Files changed: scripts/ralph/prd.json, progress.txt
---
## 2026-05-03 03:00 - US-014
- Implemented frontend data dashboard with quick search box (cross-type search across characters/skills/enemies/w_engines/disc_sets with clickable results that navigate to the corresponding page)
- Implemented full import page replacing the "coming soon" stub:
  - Scan Data Directory button calls scan_data_files Tauri command, shows available JSON files grouped by type as tag items
  - Import mode selection: Overwrite (clear + import) / Append (INSERT OR REPLACE)
  - Per-type import buttons with progress and result notifications
  - Import All button: uses reimportAll for overwrite, individual imports for append
  - Notification feedback for import results (success count + errors)
- Added scan_data_files Tauri command: scans data/ subdirectories (characters/skills/equipment/enemies/apl) for JSON files, returns grouped file list
- Added clear_data_type Tauri command: deletes all records for a specific data type (used in overwrite mode)
- Added scanDataFiles() and clearDataType() API wrappers in api.js
- Added search-results dropdown CSS (absolute-positioned below search bar with type/id/name display)
- Added 21 new i18n keys across all 3 locales (5 search + 16 import)
- Files changed: src-tauri/src/lib.rs, src/editor/pages/dashboard.js, src/editor/pages/import.js, src/editor/styles.css, src/editor/utils/api.js, src/locales/{en-US,zh-CN,ja-JP}.json, scripts/ralph/prd.json
- cargo check + vite build both pass
- **Learnings for future iterations:**
  - Patterns discovered: Import page follows the same component pattern as other editor pages. scan_data_files + clear_data_type provide a clean backend API pair for import workflows. Search results use absolute positioning within .search-bar (which needs position: relative).
  - Gotchas encountered: JSON files with trailing commas on the last entry cause vite build failures — the en-US file was safe because it had existing keys after the insertion point, but zh-CN/ja-JP needed trailing comma removal. Always verify all 3 locales after edits.
  - Useful context: reimportAll handles table clearing in FK-safe order for "overwrite all". clear_data_type handles per-type clearing with CASCADE for characters→skills. search_data already supports "all" type for cross-table search.
- Implemented frontend equipment management page with 3 sub-tabs (W-Engines, Drive Discs, Disc Sets)
- W-Engines tab: list table (ID/Name/Lv/ATK/Crit Rate/Crit DMG) + inline edit form with all stat fields + passive effects tag input (JSON.stringify array)
- Drive Discs tab: list table (ID/Slot/Lv/Set/Main Stat) + inline edit form with slot selector 1-6, main stat name/value, 4 dynamic sub-stat rows (name select + value input), set_id text field
- Disc Sets tab: list table (Set ID/Name/2pc/4pc) + inline edit form with 2pc/4pc descriptions and buff IDs
- All 3 tabs share a common inline form pattern (showInlineForm with type dispatching) and handleDelete helper
- Added editor.equipment i18n keys to all 3 locale files
- Updated scripts/ralph/prd.json US-012 → passes: true
- vite build passes (29 modules, 0 errors)
- Files changed: src/editor/pages/equipment.js, src/locales/*.json, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Patterns discovered: Sub-tab navigation uses the same `.editor-tabs`/`.editor-tab` classes as the main tab bar, with hash-based sub-routes (#/equipment/w-engines). Complex forms (W-Engine with tags, Drive Disc with sub-stats) bypass createForm and build DOM directly, then add save/cancel buttons manually — gives full control over custom sections.
  - Gotchas encountered: Always check all 3 locale files when adding new i18n keys — missing keys fall back to the key name string, which looks unprofessional. The ja-JP locale uses a mix of dotted and flat key styles — new keys should match the predominant flat style.
  - Useful context: The equipment page is the most complex of the CRUD pages with 3 sub-types, each needing distinct form layouts.
---
## 2026-05-03 02:45 - US-013
- Marked US-013 (frontend enemy config page) as passing in PRD — the implementation was already completed alongside US-012 in the working tree
- enemies.js has all 6 acceptance criteria: enemy list table (ID/Name/Type/Level/HP/DEF/Base RES), inline edit form with basic stats + dynamic resistance rows (element+value, add/delete) + weakness tag input + anomaly buildup fields, save/cancel/delete with confirmation
- Added editor.enemies i18n keys to zh-CN and ja-JP locale files (en-US already had them)
- Updated scripts/ralph/prd.json US-013 → passes: true
- vite build passes
- Files changed: src/editor/pages/enemies.js, src/locales/zh-CN.json, src/locales/ja-JP.json, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Patterns discovered: Enemies page follows the same inline edit pattern as characters/skills/equipment — anti-resistance dynamic rows are nearly identical to equipment sub-stat rows. Weakness tag input follows same pattern as passive effects tag input.
  - Gotchas encountered: The en-US locale already had editor.enemies.* keys but zh-CN and ja-JP did not — always verify all 3 locales.
  - Useful context: All frontend CRUD pages (characters, skills, equipment, enemies) now share consistent implementation patterns: hash-based routing, inline edit panels, notification helper, confirm dialog for deletes.---
## 2026-05-03 03:15 - US-015
- Replaced all hardcoded English strings in datatable.js ("Actions", "No data", "Edit", "Del") with i18n t() calls
- Replaced hardcoded defaults in confirm.js ("Cancel", "Delete") with i18n t() calls
- Replaced hardcoded defaults in form.js ("Save", "Cancel", "Add...") with i18n t() calls
- Added missing editor.action.edit/delete/add/noData/actions/search keys to en-US and zh-CN (were only in ja-JP)
- Added editor.confirm.cancel/delete and editor.form.add keys to all 3 locales
- Added editor.action.actions key to ja-JP (was missing)
- Files changed: src/editor/components/datatable.js, src/editor/components/confirm.js, src/editor/components/form.js, src/locales/{en-US,zh-CN,ja-JP}.json, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Patterns discovered: The datatable component is the single place where "Actions" column header, "Edit"/"Del" action buttons, and "No data" empty state are rendered — these should always use t() calls to support i18n. Similarly confirm.js and form.js use hardcoded English fallback strings.
  - Gotchas encountered: The ja-JP locale was ahead of en-US/zh-CN in editor.action.* keys — it had edit/delete/add/noData/search/confirmDelete while en-US and zh-CN only had save/cancel. Always cross-check all 3 locale files against the complete key set when doing i18n work.
  - Useful context: The i18n.t() function falls back to English (en-US) and then to raw key name, so missing keys in zh-CN/ja-JP silently fall back to English rather than crashing. Always rebuild and visually verify after i18n changes.
---
