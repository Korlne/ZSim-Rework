## Codebase Patterns
- Frontend pages: each page module exports `renderPage(route)` returning a DOM element; pages use inline edit panels (not modals) appended to the root container with `scrollIntoView`
- Shared components: place reusable UI in `src/editor/components/`, utility functions in `src/editor/utils/`
- Frontend validation: use `src/editor/utils/validation.js` (validateCharId, validateLevel, validateCritRate, validateNonNegative, validateForm)
- Number formatting: use `src/editor/utils/format.js` (formatStat for table display, formatFixed for precision)
- SQLite schema: use `CREATE TABLE IF NOT EXISTS` for idempotent init + `schema_version` table for migrations
- rusqlite: always use `bundled` feature to avoid requiring system SQLite install
- data_entry module: declare `mod data_entry;` in lib.rs, sub-modules in `data_entry/` directory
- Cascade deletes: use `ON DELETE CASCADE` in FK definitions + `PRAGMA foreign_keys = ON;`
- JSON in SQLite: store complex nested data as TEXT columns, parse with serde_json on read
- DataLoader pattern: struct holds `rusqlite::Connection`, load methods take `&self`, complex entity reconstruction via JOINs
- Enum serialization: store enums as serde-rename strings in TEXT columns, deserialize with `serde_json::from_str(&format!("\"{}\"", value))`
- from_json_dir: embed schema + JSON import logic directly on DataLoader for self-contained bootstrap from flat files
- Editor pattern: hash-based routing (#/route) with lazy-loaded page modules via dynamic import(), sidebar with active state tracking, shared components (datatable, form, confirm) in src/editor/ root
- i18n system: t(key, params) in src/i18n.js, flat key-value JSON in src/locales/{lang}.json, data-i18n DOM attributes for static HTML, "langchange" custom event for dynamic re-render
- CSV import: calamine crate for XLSX parsing in src-tauri/src/data_entry/csv_import.rs, functions return Result<usize, String>
- Tauri commands: #[tauri::command] in lib.rs, registered via generate_handler![], state managed via app.manage() with Mutex<T> for interior mutability
- Simulation: run_simulation spawns std::thread with app.clone() to emit progress events, SimulationState with Arc<AtomicBool> for cancellation, config passed as JSON string
- Tauri setup hook: use `.setup(move |_app| { ... Ok(()) })` for startup tasks (auto-import, etc.); runs before any commands; clone PathBuf if you also need it for managed state

---
## 2026-05-08 - PRD created
- Branch: ralph/i18n-sim-config-csv-import
- 12 user stories (US-101 to US-112) covering: CSV auto-import, sim config mode, i18n enum translation (all pages), sim count default view, disc set hierarchy UI

## 2026-05-08 - US-101
- What was implemented: Added `auto_import_on_first_startup()` function and Tauri `.setup()` hook in lib.rs. On first startup, checks if `data/zsim.db` exists; if not, creates schema via `init_db`, then scans `data/characters/`, `data/equipment/`, `data/wengine/` for `.csv`/`.xlsx` files and imports them in order: characters → drive discs → w-engines. Errors logged with `eprintln!` and do not block startup.
- Files changed: `src-tauri/src/lib.rs` (+78 lines)
- **Learnings for future iterations:**
  - Tauri v2 `.setup()` hook takes `|app| Ok(())` closure and runs before any commands are invoked
  - When using `move` closures with `setup()` and managed state, clone `PathBuf` first because the closure consumes it
  - CSV/XLSX files are stored in subdirectories: characters/ → `data/characters/`, drive_discs → `data/equipment/`, w_engines → `data/wengine/`
  - `calamine` crate used for XLSX parsing — opens both `.csv` and `.xlsx` files transparently
---

## 2026-05-08 - US-102
- What was implemented: Updated `run_simulation` Tauri command to parse `mode` ("full"/"loop") and `loop_count` from config JSON. Full mode runs APL once (current behavior). Loop mode scales effective workload by `loop_count`. Added `Full` and `Loop` variants to `SimMode` enum in zsim-core with serde annotations. Added `loop_count: u64` field to `SimConfig` with default 1. Updated completion message to show loops in loop mode. Backward compatible: missing mode field defaults to "full".
- Files changed: `src-tauri/src/lib.rs`, `zsim-core/src/entities/enums.rs`, `zsim-core/src/combat/runner.rs`, `zsim-core/src/combat/parallel.rs`
- **Learnings for future iterations:**
  - Frontend already sends `mode` and `loop_count` in config JSON (main.js getConfig()) — the Rust side was the missing piece
  - `SimMode` has two axes: execution type (Single/Parallel) and termination type (Full/Loop) — need to keep both
  - When adding a field to a widely-used struct like `SimConfig`, grep for all construction sites including tests and parallel runner
  - Pre-existing test failures in `data::loader` and `zsim-parquet` e2e tests due to `potentials` column mismatch — not related to sim config
---

## 2026-05-08 - US-103
- What was implemented: Added full enum translation keys for all 6 categories (11 factions, 5 specialties, 5 elements, 9 action types, 11 stat types, 3 enemy types) to zh-CN.json, en-US.json, and ja-JP.json. Created `src/editor/utils/i18n-maps.js` exporting FACTION_MAP, SPECIALTY_MAP, ELEMENT_MAP, ACTION_TYPE_MAP, STAT_MAP, and ENEMY_TYPE_MAP mapping objects (value → i18n_key).
- Files changed: `src/locales/zh-CN.json`, `src/locales/en-US.json`, `src/locales/ja-JP.json`, `src/editor/utils/i18n-maps.js` (new)
- **Learnings for future iterations:**
  - Enum translation keys follow pattern `enum.<category>.<snake_case_value>` (e.g., `enum.faction.belobog_industries`)
  - i18n-maps.js mapping objects map from the raw English enum value (as stored in DB) to the i18n key — usage: `t(MAP[rawValue])`
  - When adding new enum values, both the locale files AND i18n-maps.js need updating
---
