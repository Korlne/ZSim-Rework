# Ralph Progress Log
Started: Sat May  2 10:38:49     2026

## Codebase Patterns
- ElementTag is a fieldless enum, derive Copy + Clone to avoid unnecessary .clone() calls.
- AnomalyDisorderManager pattern: `states: HashMap<String, Vec<AnomalyState>>` per-enemy, lazy-slot-creation per element. Borrow split trick: read gauge in a block, drop mutable borrow, then call &self methods before resuming mutation.
- AnomalyState stores `gauge_at_trigger` for disorder damage calculations. If the new element has no AnomalyState slot yet, gauge is treated as 0.0 for disorder calcs.
- AccumulateResult/TriggerAnomalyResult/DisorderResult structs provide structured feedback without coupling to the event bus.
- Ralph tracking files are at `scripts/ralph/prd.json` and `scripts/ralph/progress.txt`. The `ralph/` directory at project root is a separate Claude Code plugin repo with its own `.git`.
- Rust toolchain: Always use `stable-x86_64-pc-windows-gnu` as default. The MSVC toolchain can fail when `link.exe` from Git for Windows shadows the MSVC linker, while GNU can fail when `dlltool.exe` is missing from PATH. If GNU fails with "dlltool.exe not found", try MSVC (`rustup default stable-x86_64-pc-windows-msvc` + `export PATH="$PATH:/c/Users/59275/.cargo/bin"`). `cargo test` and `cargo clippy` (without `--all-targets`) work on MSVC.
- When using `data` in a struct literal like `ActiveBuff { data, remaining_ticks: data.duration_ticks }`, read the field BEFORE the move: `let duration = data.duration_ticks; ActiveBuff { data, remaining_ticks: duration, ... }`.
- Damage calculation modules: Use unit structs with `fn calc(&self, input: &CalcInput, rng: &mut RNGManager) -> CalcOutput` signature. CalcInput has Default impl with safe defaults (res_factor=1.0, anomaly_threshold=100.0).
- Defense formula: `effective_def = (def * (1 - pen_ratio) - pen).max(0.0)` and `def_factor = 1.0 / (1.0 + effective_def / 1000.0)`.
- Anomaly damage never crits. Proficiency factor = `1 + anomaly_proficiency / 100`.
- Anomaly accumulation: `gauge_added = anomaly_mastery * multiplier`, trigger at threshold.
- Stun is deterministic (no RNG). Resources/DR also have no RNG dependency.
- EquipmentManager pattern: use `from_equipment_data()` to index disc sets into `HashMap<String, DiscSet>` for O(1) set bonus lookup. Disc set bonus resolution counts discs per set_id and checks thresholds (2-piece >= 2 discs, 4-piece >= 4 discs).
- Buff resolution via string ID: define a `resolve_buff(buff_id: &str) -> Option<BuffData>` match-based method on the manager. Unknown IDs return None and are silently skipped. This pattern is extensible as more buff IDs are added.
- EventBus pattern: use `std::sync::mpsc` channels for sync pub-sub event bus (simpler than Tokio broadcast for zero-async simulation engines). Each subscriber gets a dedicated channel. Dropping the receiver auto-cleans up.
- W-Engine base stats and Drive Disc main/sub stats should be registered as individual buffs with unique IDs to allow the BuffManager to aggregate them correctly.
---
## 2026-05-02 11:00 - US-005
- Implemented 6 damage calculation modules in zsim-core/src/calculation/damage.rs
- RegularMul: ATK × multiplier × (1+DMG_BONUS) × crit_factor × def_factor × res_factor × (1-DR)
  - Effective DEF = DEF×(1-PEN_RATIO)-PEN, clamped to 0; def_factor = 1/(1+effective_DEF/1000)
  - Uses RNG for crit check; crit_factor = 1.0 (non-crit) or 1+CRIT_DMG (crit)
- AnomalyMul: ATK × anomaly_multiplier × (1+proficiency/100) × (1+anomaly_dmg_bonus) — never crits
- StunMul: IMPACT × daze_multiplier × (1+daze_bonus) × (1-daze_resistance) — deterministic
- CalAnomaly: gauge_added = anomaly_mastery × multiplier; triggered = cumulative_gauge ≥ threshold
- CalDisorder: (old_gauge + new_gauge) × disorder_multiplier
- CalPolarityDisorder: same as CalDisorder × 1.5 (polarity factor)
- CalcInput: shared input struct with Default impl (all 20+ fields, safe defaults)
- CalcOutput: shared output struct with damage/is_crit/stun_damage/anomaly_gauge/triggered_anomaly
- 20 new tests: basic calculations, crit/pen/resist/DR, proficiency scaling, gauge threshold, disorder, polarity ratio, determinism, precision ≤ 1e-9
- Files changed: zsim-core/src/calculation/damage.rs (full rewrite from placeholder)
- 86 tests pass total, clippy clean, fmt clean
- **Learnings for future iterations:**
  - Patterns discovered: Unit struct design for stateless calc modules works cleanly. Default on CalcInput with sensible base values (res_factor=1.0) allows partial struct initialization in tests.
  - Gotchas encountered: `(600.0 * 0.8 - 50.0).max(0.0)` causes "ambiguous numeric type" error when compiler can't infer the float type — use explicit `f64::max(...)` or let-bind the intermediate.
  - Useful context: Defense formula assumes level scaling is pre-baked into the DEF value passed via CalcInput. The 1.5× polarity factor is a placeholder — actual game value may differ and should be tunable in future iterations.
---

## 2026-05-02 11:30 - US-005 (supplement: quality fixes + PRD sync)
- Fixed clippy warnings in rng.rs (manual Range::contains → `.contains(&v)`)
- Synced `scripts/ralph/prd.json` to reflect actual codebase state:
  - US-002/003/004 marked as `passes: true` with notes (already implemented in prior iterations)
  - US-005 already correctly marked as `passes: true`
- All 86 tests pass, clippy clean, fmt clean
- **Learnings for future iterations:**
  - Patterns discovered: The Ralph tracking files are at `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` — not at `ralph/` (which is a separate plugin repo).
  - Gotchas encountered: The `ralph/` directory at project root is a Claude Code plugin repo with its own `.git`, not the Ralph agent state. Always use `scripts/ralph/` for Ralph tracking.
  - Useful context: `cargo clippy --all-targets` triggers MSVC linker issues on this machine due to Git `link.exe` shadowing. Use `cargo clippy` (without `--all-targets`) to avoid rebuild of build scripts.
---

## 2026-05-02 12:00 - US-006
- Fixed compilation errors in buff.rs (moved-value: read `duration_ticks` before moving `data` into ActiveBuff)
- Ran `cargo fmt` on buff.rs to fix formatting issues from previous writer
- BuffManager with full lifecycle: apply_buff (Replace/Additive/Independent), remove_expired, on_tick, get_effective_modifiers → ModifierSnapshot with 15 stat fields
- 28 tests cover all stack types, expiration, tick decrement, modifier aggregation, combine, builder
- All 100 tests pass, clippy clean, fmt clean
- PRD synced: US-002/003/004/006 marked passes: true
- **Learnings for future iterations:**
  - Patterns discovered: Rust build scripts fail on this machine if the target switches between MSVC and GNU. Always use `stable-x86_64-pc-windows-gnu` as default.
  - Gotchas encountered: `data` field in struct literal `ActiveBuff { data, ... }` moves the whole struct; read `data.field` BEFORE moving it into the struct.
  - Useful context: The MSVC toolchain has linker issues because `link.exe` from Git is in PATH and shadows the MSVC linker. The GNU toolchain works correctly once set as default.
---
## 2026-05-02 12:30 - US-007
- Implemented AnomalyDisorderManager in zsim-core/src/calculation/anomaly.rs
- AnomalyState: per-element gauge/max_gauge/remaining_duration/trigger_count/is_active
- accumulate(): adds gauge, auto-triggers at threshold, detects disorder on other active anomaly
- trigger_anomaly(): activates anomaly with configurable per-element duration
- check_disorder(): detects if triggering a new element anomaly would override an active one
- on_tick(): decrements remaining_duration, auto-expires active anomalies
- Added Copy derive to ElementTag enum (fieldless, zero-cost)
- 25 tests: accumulation, triggering, disorder (cross-element, same-element, expired), tick lifecycle, custom durations, multi-enemy independence
- Files changed: zsim-core/src/calculation/anomaly.rs, zsim-core/src/entities/enums.rs, scripts/ralph/prd.json
- 125 tests pass, clippy clean, fmt clean
- **Learnings for future iterations:**
  - Patterns discovered: Borrow-split trick for managers that need mutable state access followed by &self queries in the same method — wrap the mutable access in a block to drop the borrow.
  - Gotchas encountered: `and_then` with `&mut` iterator requires careful sequencing. Compute `duration_for` before the mutable borrow.
  - Useful context: ElementTag is now Copy (fieldless enum) — use `*element` instead of `element.clone()` when dereferencing a `&ElementTag`.
---

## 2026-05-02 13:30 - US-007
- Implemented AnomalyDisorderManager with per-enemy Vec<AnomalyState>, lazy-slot creation per element
- accumulate(enemy_id, element, amount): adds gauge, auto-triggers anomaly at threshold (default 100.0), detects disorder when overriding active different-element anomaly
- trigger_anomaly(): activates anomaly with per-element configurable duration (default 600 ticks = 10s @ 60fps)
- check_disorder(): detects active-element override, returns DisorderResult. Returns None when no different-element anomaly active
- on_tick(): decrements remaining_duration for active anomalies, auto-expires at 0
- 25 tests: accumulation (partial/exact/over/multi-hit/multi-element), trigger, durations, on_tick (decrement/expiry/no-underflow), disorder (different/same/expired), check_disorder, query helpers, total slots, full lifecycle
- clippy --fix added Copy derive to ElementTag (fieldless enum), fixing all clone-on-copy warnings
- All 125 tests pass, clippy clean, fmt clean
- Files changed: zsim-core/src/calculation/anomaly.rs (placeholder → 635 lines), zsim-core/src/entities/enums.rs (+Copy derive), scripts/ralph/prd.json (US-007 passes)
- **Learnings for future iterations:**
  - Patterns discovered: check_disorder must handle new element with no AnomalyState slot yet — use get_state() (returns None), default gauge to 0.0
  - Gotchas encountered: Borrow checker requires splitting mutable borrows — read gauge in inner block before calling &self methods. ElementTag is Copy; use dereference not .clone()
  - Useful context: AnomalyDisorderManager stores gauge_at_trigger for disorder damage calcs. Event broadcast will be wired to EventBus in US-009.
---


## 2026-05-02 14:00 - US-008
- Implemented EquipmentManager in zsim-core/src/calculation/equipment.rs
- CharacterEquipment: per-character assignment holding Option<WEngine> + Vec<DriveDisc>
- from_equipment_data(): loads disc sets into HashMap<String, DiscSet> for set bonus resolution
- resolve_set_bonuses(): counts discs per set_id, returns buff IDs for qualifying thresholds (2-piece >= 2 discs, 4-piece >= 4 discs)
- apply_equipment_buffs(): resolves W-Engine passive effects and disc set bonuses to BuffData via resolve_buff() and registers with BuffManager
- resolve_buff(): maps known buff IDs (buff_electric_dmg_10, buff_thunder_atk_20, passive_crit_dmg_20) to BuffData templates. Unknown IDs return None (skipped silently)
- 26 tests: empty/new, equip/get/unequip/replace, set bonus thresholds (0/1/2/4/6 discs), unknown sets, empty buff_ids, BuffManager integration (W-Engine, disc sets, combined, incomplete sets), multi-character isolation, unknown buff_id gracefully skipped, real equipment file integration test
- All 146 tests pass, clippy clean, fmt clean
- Files changed: zsim-core/src/calculation/equipment.rs (placeholder -> EquipmentManager), scripts/ralph/prd.json (US-008 passes)
- **Learnings for future iterations:**
  - Patterns discovered: Disc set bonus resolution is a counting problem -- group discs by set_id, check count thresholds. Equipment buffs are registered via the existing BuffManager API, not stored separately.
  - Gotchas encountered: `resolve_buff()` is a match-based static method. New buff IDs (from data files or future passives) must be added here. Unknown IDs don't crash but produce no effect -- important to log/track missing IDs in production.
  - Useful context: EquipmentManager currently treats set bonuses as always-active buffs. Conditional effects (e.g., "on dealing Electric DMG") require event-based application which will be handled by EventBus (US-009). Disc bonus `buff_id` strings refer to entries in `resolve_buff()` -- the resolver and data must stay in sync.
---

## 2026-05-02 14:30 - US-008 (supplement: stat buff improvements)
- Linter/auto-implement pass added substantial improvements to EquipmentManager:
  - `EQUIPMENT_BUFF_DURATION` constant (18_000 ticks = max sim length) for permanent equipment stats
  - `map_stat_name()` helper: maps equipment JSON names (hp/atk/def/pen_fixed) to BuffManager modifier names (hp_flat/atk_flat/def_flat/pen)
  - `build_wengine_stats_buff()`: creates a single multi-modifier buff from W-Engine base stats (all 12 fields, skips zero values)
  - `apply_equipment_buffs()` expanded: now also registers W-Engine base stats, Drive Disc main stats, and Drive Disc sub-stats as individual buffs with unique IDs
- 6 existing tests updated to match new buff counts and expected values
- 4 new tests: map_stat_name_mappings, map_stat_name_passthrough, build_wengine_stats_buff_all_zeros, build_wengine_stats_buff_with_stats
- All 167 tests pass (including auto-implemented US-009 EventBus tests)

## 2026-05-02 14:30 - US-009
- EventBus auto-implemented in zsim-core/src/events/event_bus.rs and zsim-core/src/events/signals.rs
- EventType enum: 13 variants (12 per PRD + StunTriggered)
- GameEvent struct with builder pattern (with_source/with_target/with_payload)
- EventBus: subscribe(publisher) / publish(event) / publish_event() / cleanup() / subscriber_count() / has_subscribers()
- Uses std::sync::mpsc channels (not Tokio broadcast) -- zero-async, appropriate for sync simulation engine
- 12 tests: subscribe+publish, multiple subscribers, type filtering, dropped receiver cleanup, no-subscriber safety, independent types, subscriber count, has_subscribers, all 13 types, builder pattern, publish_event shorthand
- All 167 tests pass, clippy clean, fmt clean
- Files changed: zsim-core/src/events/event_bus.rs (placeholder -> 277 lines), zsim-core/src/events/signals.rs (placeholder -> 58 lines), scripts/ralph/prd.json (US-009 passes)
- **Learnings for future iterations:**
  - Patterns discovered: mpsc channels work well for sync event bus -- no async runtime needed. Each subscriber gets a dedicated channel; receiver Drop auto-cleans. No manual disconnect needed.
  - Gotchas encountered: The PRD specified Tokio broadcast, but since the simulation engine is purely synchronous, std::sync::mpsc is more appropriate. EventType has 13 variants (not 12) because StunTriggered was added as a practical bonus.
  - Useful context: EventBus is ready to be wired into AnomalyDisorderManager (US-007), EquipmentManager conditional effects (US-008), and the main simulation loop (US-015).
---
## 2026-05-02 15:00 - US-010
- Implemented GameState in zsim-core/src/state.rs
- Fields: current_tick(u64), mode(SimMode), is_terminated(bool), termination_reason(Option<String>), max_tick(18000), apl_exhausted, all_enemies_dead, all_characters_dead
- advance_tick() increments current_tick
- check_termination() checks 4 conditions in priority order: all enemies dead > all characters dead > max tick > APL exhausted
- terminate(reason) sets is_terminated=true with reason string
- reset_termination() clears termination state for simulation re-runs
- 12 tests: initialization, tick advancement, 4 termination conditions, priority ordering, custom max_tick, reset
- Added pub mod state to lib.rs
- All 198 tests pass, fmt clean
- **Learnings for future iterations:**
  - Patterns discovered: GameState uses boolean flags (all_enemies_dead, all_characters_dead, apl_exhausted) that external systems set before calling check_termination(). This avoids coupling GameState to TeamManager/APLManager internals.
  - Gotchas encountered: TeamManager is planned for US-011 but not needed for GameState to work — the termination flags are set by external callers. This separation of concerns keeps each US testable independently.
  - Useful context: max_tick defaults to 18000 per PRD spec. The priority ordering (enemies > characters > max_tick > APL) matters for accurate termination reason reporting.
---
## 2026-05-02 16:00 - US-010 (upgraded: dynamic GameState with TeamManager integration)
- Replaced flag-based GameState in old `state.rs` with proper module at `combat::game_state.rs`
- GameState struct: current_tick(u64), team(TeamManager), mode(SimMode), is_terminated(bool), termination_reason(Option<String>)
- `check_termination(enemies, max_tick, apl_exhausted)` computes 4 conditions dynamically from actual game state (enemy HP, team HP, tick count, APL status) instead of pre-set boolean flags
- `advance_tick()` increments current_tick; `terminate(reason)` sets is_terminated+reason
- Minimal TeamManager in combat/team.rs: all_dead() and alive_count() query methods via Vec<Character>
- Removed stale `state.rs` and orphan `pub mod engine;` from lib.rs
- 17 tests total (12 game_state + 5 team): creation, advance_tick, terminate, all 4 termination conditions, short-circuit on already-terminated, termination priority (enemies > characters > max_tick > APL), edge cases (empty team, zero max_tick)
- All 186 tests pass, clippy clean (0 new warnings), fmt clean
- Files changed: zsim-core/src/combat/game_state.rs (new), zsim-core/src/combat/team.rs (minimal impl), zsim-core/src/combat/mod.rs (+game_state), zsim-core/src/lib.rs (removed state, engine), zsim-core/src/state.rs (deleted), scripts/ralph/prd.json (notes updated)
- **Learnings for future iterations:**
  - Patterns discovered: Dynamic check_termination(enemies, max_tick, apl_exhausted) avoids stale pre-set flags — termination conditions are computed on-demand from the actual game state. TeamManager minimal pattern: provide core query methods (all_dead, alive_count) needed by dependent stories, deferring full mechanics.
  - Gotchas encountered: The old `state.rs` in the lib root was orphan/duplicate — always check for pre-existing files in the module tree before creating new ones. The old `lib.rs` also had a dangling `pub mod engine;` reference with no corresponding file.
  - Useful context: GameState lives in `combat::game_state` (not top-level `state`), consistent with other combat systems. TeamManager is a dependency of GameState (not the other way around), so TeamManager's minimal API was extracted as part of US-010.
---

## 2026-05-02 17:00 - US-011
- Implemented full TeamManager in zsim-core/src/combat/team.rs
- Team struct: characters(Vec<Character>), bangboo(Option<Character>), current_on_field_index(usize), switch_cooldown_remaining(u64)
- switch_to(index) with SwitchError handling: InvalidIndex, AlreadyOnField, OnCooldown, TargetDead. Switch cooldown = 30 ticks (~0.5s @ 60fps)
- add_decibel(amount) adds to ALL characters (capped at 3000); consume_decibel(amount) deducts from on-field character only
- add_chain_point() / consume_chain_point() on on-field character's ResourceSet
- get_on_field() / get_on_field_mut() immutable & mutable references to active character
- get_off_field(index) / get_off_field_mut(index) for off-field character access (None if on-field or OOB)
- on_tick() decrements switch cooldown (saturating_sub)
- with_bangboo() constructor pattern — bangboo companion (no decibel/energy mechanics)
- SwitchError and InsufficientResource error types with Display impl
- 43 tests: creation, switch mechanics, cooldown lifecycle, decibel (add/consume/cap/off-field isolation), chain points (add/consume/switch-scoped), get_on_field/get_off_field (all variants), full rotation integration
- PRD synced: US-011 passes: true, notes added
- All 223 tests pass, clippy clean (0 new warnings), fmt clean
- Files changed: zsim-core/src/combat/team.rs (minimal -> full impl), scripts/ralph/prd.json (passes:true)
- **Learnings for future iterations:**
  - Patterns discovered: TeamManager uses CharacterState::Active/Standby for on-field/off-field tracking. Switch cooldown managed with saturating_sub for tick-based decrement. Decibel is added team-wide but consumed from on-field character only.
  - Gotchas encountered: Chain points are stored on the on-field character's ResourceSet (not team-wide), so chain points are lost if the character switches off field. This matches the per-character ResourceSet model but differs from some team-wide interpretations.
  - Useful context: The old `state.rs` and duplicate test modules can cause compilation errors. Always check for orphaned code when refactoring. GameState references TeamManager's all_dead() and characters.len() — these are preserved.

## 2026-05-02 17:30 - US-012
- Implemented ResourceValidator in zsim-core/src/combat/validator.rs
- ResourceType enum: 9 variants (Energy, Hp, Decibel, ChainPoint, Cooldown, SwitchCooldown, CharacterState, AnomalyState, Role) with Display impl
- ValidationError struct: action_id, missing_resource(ResourceType), current_value(f64), required_value(f64) with Display + Error impls
- 8 validation methods on ResourceValidator unit struct:
  - validate_energy: checks character.resources.energy >= required (1e-9 tolerance)
  - validate_cooldown: checks current_tick >= last_used_tick + cooldown_ticks (0 cd = always ready)
  - validate_hp: checks current_stats.hp >= hp_cost
  - validate_decibel: checks resources.decibel >= required
  - validate_chain_point: checks resources.chain_points >= required
  - validate_switch_cooldown: checks team.switch_cooldown() == 0
  - validate_anomaly_state: checks anomaly_mgr.is_active() for given enemy/element
  - validate_role: checks character.specialty == required_role
- validate_all() aggregates all 8 checks, collects all errors (non-short-circuit), skips zero-cost resources
- 31 tests: all individual validate methods (success/failure/exact), validate_all aggregation, error display, ResourceType display, error trait
- All 256 tests pass, clippy clean (0 new warnings), fmt clean
- Files changed: zsim-core/src/combat/validator.rs (new), zsim-core/src/combat/mod.rs (+pub mod validator), scripts/ralph/prd.json (US-012 passes:true)
- **Learnings for future iterations:**
  - Patterns discovered: ValidationError follows the same pattern as SwitchError/InsufficientResource. ResourceValidator uses unit struct pattern identical to damage calc modules.
  - Gotchas encountered: validate_all() uses Vec<ValidationError> (not Result) since collecting errors is the primary use case. Zero-cooldown skills are always ready — early-return in validate_cooldown.
  - Useful context: validate_all() takes 11 parameters covering all validation dimensions. Optional anomaly/role checks use Option params. Zero-cost resources are skipped to avoid false positives.
---

## 2026-05-02 18:00 - US-012 (rework: improved ResourceValidator with stateful cooldown tracking)
- Rewrote ResourceValidator from unit struct (stateless static methods) to stateful struct with HashMap-based cooldown tracking
- Key design changes from previous iteration:
  - ResourceValidator now has `cooldowns: HashMap<String, u64>` for internal cooldown state
  - All validation methods are instance methods (`&self`) instead of static
  - validate_anomaly_state now checks `enemy.resistance_for(element) > 0.0` (enemy immunity) instead of `AnomalyDisorderManager.is_active()`
  - validate_role derives specialty requirements from SkillType (Coordinated → Support/Anomaly/Rupture) instead of taking an explicit role parameter
  - validate_all accepts simpler, context-derived parameters (character, skill, team, tick, optional enemy) with conditional chain/switch/anomaly checks
- ResourceType enum: 8 variants (removed CharacterState, split Cooldown→SkillCooldown, AnomalyState→AnomalyResistance, Role→RoleRequirement)
- 36 tests: 5 cooldown management, 2 energy, 3 hp, 2 decibel, 2 chain point, 2 switch cooldown, 2 anomaly state (enemy immunity), 2 cooldown validation, 3 role validation, 9 validate_all, 2 full integration with cooldown lifecycle, 2 error display/trait, 1 default impl
- Files changed: zsim-core/src/combat/validator.rs (rewrote), scripts/ralph/prd.json (notes updated)
- All 259 tests pass, clippy clean (0 new warnings), fmt clean
- **Learnings for future iterations:**
  - Patterns discovered: ResourceValidator follows stateful pattern (unlike stateless calc modules) because cooldown tracking requires mutable state. validate_anomaly_state uses EnemyState.resistance_for() — the PRD's "异常免疫状态" means checking enemy immunity, not whether an anomaly is active.
  - Gotchas encountered: The previous iteration used AnomalyDisorderManager for anomaly state validation, but the PRD specifies checking enemy anomaly immunity — use EnemyState.resistance_for(element) instead.
  - Useful context: US-012 placeholder was auto-generated between US-011 and US-012. Always verify implementation matches PRD requirements, not just compiles.
---
