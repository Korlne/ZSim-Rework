# Ralph Progress Log
Started: Sat May  2 15:34:32     2026
---

## 2026-05-02 - US-004
- Implemented Character CRUD backend: get_characters, get_character, save_character, delete_character
- Added characters.rs with full char_id validation (lowercase + digits + underscores only), numeric range validation for level/ascension/crit rates
- 16 unit tests covering: empty list, single/multiple ordered, get with skills, save new, save update, validation errors (empty id, invalid format, level range, crit range), delete, delete not found, cascade delete
- Tauri commands registered in lib.rs with DataDirState pattern matching existing commands
- **Learnings for future iterations:**
  - Use `cmd_` prefix for Tauri command wrappers that receive owned Strings, delegate to the core function that takes `&str`
  - Always enable `PRAGMA foreign_keys = ON;` per connection — it's off by default in rusqlite
  - The data_entry pattern: functions accept `&Connection`, Tauri commands accept `State<DataDirState>`, open connection, set pragma, then call the module function
---
